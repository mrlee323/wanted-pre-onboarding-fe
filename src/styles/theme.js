const color = {
  active: '#0095f6',
  disabled: '#b2dffc',
};

const size = {
  mobile: '650px',
};

const media = {
  max: `(max-width: ${size.mobile})`,
  min: `(min-width: ${size.mobile})`,
};

const border = {
  main: '1px solid #ccc',
  sub: '1px solid #eee',
};

export const theme = {
  color,
  size,
  media,
  border,
};
